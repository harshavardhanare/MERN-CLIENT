import React from 'react'

export const Effects = ({course}) => {
  return (
    <div><img src={course.image.url}></img>
    <div>
        <button>
            
        </button>
        </div></div>
  )
}

